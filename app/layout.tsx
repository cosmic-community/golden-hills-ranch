import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Golden Hills Ranch - Premium Grass-Fed Beef',
  description: 'Family-owned Montana ranch raising premium grass-fed beef for over 70 years. Shop our selection of steaks, ground beef, and roasts.',
  keywords: 'grass-fed beef, Montana ranch, premium steaks, ground beef, roasts, family farm',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}